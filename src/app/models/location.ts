export class Location {
    region: string
    city: string
    suburb: string

    constructor(region: string, city: string, suburb: string){
        this.region = region
        this.city = city
        this.suburb = suburb
    }
}
